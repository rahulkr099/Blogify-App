<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./partials/head'); %>
    <title>Blog</title>
  </head>
  <body data-bs-theme="dark" class="text-white">
    <%- include('./partials/nav') %>
    <div class="container mt-4"> 
      <h1 class="text-uppercase"><%=blog.title%></h1>
      <img class="rounded" src="<%= blog.coverImageURL %>" width="500px"/>
      <div class="d-flex m-2">
        <img class="rounded-circle border border-white" src="<%= blog.createdBy.profileImageURL %>" width="40px"/>
        <h4 class="m-2 p-2">Created By: <%= blog.createdBy.fullName %> </h4>
      </div class="card-body">
      <pre class=" mb-2 mt-5 border border-primary rounded shadow p-4"><%= blog.body %></pre>
    </div>
    <div class="container mt-3">
    <!-- comments variable backend se pass hua h, routes me jaakar dekho -->
      <h1 class="text-primary">Comments (<%= comments.length %>)</h1>
<!-- locals ke andar saare variable store rhte h, to tum  -->
<!-- piyush ka backend ka video 31 ka 15:00 se dekh lo -->
      <% if (locals.user) { %>
        <form action="/blog/comment/<%= blog._id %>" method="post">
            <div class="mb-3 w-50">
              <input
                type="text"
                name="content"
                class="form-control"  
                placeholder="Enter Your Comment"         
              />
            </div>
            <button type="submit" class="btn btn-primary mb-3">ADD</button>
          </form>
      <% } %>
    </div>
    <div class="container">
<!-- hum comments ko backend se frontend me pass kr rhe h, isliye locals nhi use kiye -->
        <% comments.forEach(comment => { %>
           <img src="<%= comment.createdBy.profileImageURL %>" width="30px" class="rounded-circle m-1 border border-danger"/> 
             <%=comment.createdBy.fullName %>:
      <pre class="border p-2 rounded w-50"><%= comment.content %></pre>
        <% }) %>
    </div>
    <%- include('./partials/script') %>
  </body>
</html>
